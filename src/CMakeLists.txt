if (BUILD_SMASH)
    add_subdirectory(Network)
    add_subdirectory(Engine)
    add_subdirectory(Smash)
else()
    add_subdirectory(Network)
    add_subdirectory(Engine)
    add_subdirectory(RType)
endif()
